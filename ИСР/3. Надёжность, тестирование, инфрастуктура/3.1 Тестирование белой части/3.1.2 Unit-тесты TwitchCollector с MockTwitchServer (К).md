Требование:
Написать unit-тесты для TwitchCollector, используя уже существующий MockTwitchServer
и FakeResponser (они уже в коде, но тестов к ним нет).

Покрыть сценарии:
  1. collectSelected() с метриками из streams (viewers, title, game, language, is_mature,
     started_at, type, status) → все поля корректно распарсились из FakeResponser.getStreamJson()
  2. collectSelected() с метриками из users (display_name, description) → корректный парсинг
  3. collectSelected() с метрикой followers → корректный парсинг total из getFollowersJson()
  4. collectSelected() — канал оффлайн (data: [] в streams) → status=offline, viewers=0
  5. collectSelected() — комбинация метрик из разных источников → все три запроса выполнены
  6. collectSelected() — запрошена только одна метрика, не требующая остальных источников
     → лишние HTTP-запросы не делаются (проверить через счётчик запросов в MockWebServer)
  7. Сетевая ошибка (MockWebServer возвращает 500) → RuntimeException с понятным сообщением

Ожидаемое время выполнения: 1-2 дня
Какие задачи должны быть завершены для того, чтобы взять это задание:
[[1.1.1.2 Реализация конечной точки сбора API (К)]]