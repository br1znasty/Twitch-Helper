Требование:
Описать в тех. доке полную архитектуру серой части системы:
  1. Какие компоненты входят в систему: сервер управления, боты, прокси, БД, UI
  2. Схема взаимодействия:
       UI → Сервер управления → Диспетчер ботов → Бот → Прокси → Twitch
  3. Протокол команд: какие команды сервер может отдавать боту
     (подключиться к стриму, отправить сообщение, отключиться, сменить прокси)
  4. Описать, как боты получают команды: polling, WebSocket или очередь задач
  5. Описать жизненный цикл бота: инициализация → подключение → активность → отключение
  6. Зафиксировать форматы запросов/ответов (JSON-схемы)

Ожидаемое время выполнения: 2-3 дня
Какие задачи должны быть завершены для того, чтобы взять это задание:
(нет зависимостей)